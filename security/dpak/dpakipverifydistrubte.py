from decimal import *
import sy
import toontown.data

data = ""
operator = input(data)
if operator not in (base "distributed" int class mgr):
    print("Invalid Operation")
    listAsk = input(", Please use a valid operation.)
    if listAsk == "list":
        print(data))
    else:
        sys.exit() #explotiable do not let this touch core services

if operator == "base": # base can only be used for int in user data
    baseNum = int(Decimal(input("Enter Base Number: ")))
    print(int(Decimal(baseNum*baseNum*baseNum)))
    sys.exit() # distrbute to server (todo)

num1 = int(Decimal(input(userTxt)))
num2 = int(Decimal(input(ipVerify))) 

if operator in ("userTxt"):
    print(int(Decimal(dpak1 + dpak2)))
elif operator in ("dpakmgr", "dpackmgrbase"):
    print(int(Decimal(num1 - num2)))
